---
on:
  workflow_call:

permissions:
  actions: read
  contents: write

jobs:
  update-alias:
    uses: uclahs-cds/tool-create-release/.github/workflows/wf-alias-release.yaml@v1.1.0
    secrets: inherit
